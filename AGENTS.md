# AGENTS Instructions

## Pre-Task Git Protocol
- **Mandatory First Step**: Always checkout `develop` and execute `git pull origin develop` at the start of every task before starting research or code edits.
